generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL") // Agora isso VAI funcionar
}

model Profile {
  id   String @id @default(uuid())
  name String
  description String
}